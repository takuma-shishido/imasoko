// デザイントークン(色パレットの一元管理・docs/02)。
// ここは TS/TSX のインラインスタイル/SVG 属性で使う色の集約先。
// CSS 変数(styles/tokens.css)とは別レイヤ。値は既存の hex と完全一致させており、
// 表示色は一切変えない(issue #106 は純粋な同値リファクタ)。
//
// ※ WHITE は元コードの "#fff" と "#ffffff" の両方を統合(CSS 上どちらも同じ白)。

export const COLORS = {
  /** 主要テキスト・濃い前景・自分マーカー */
  INK: "#171717",
  /** 副次テキスト(やや薄い前景) */
  SUBTLE: "#4d4d4d",
  /** 補助テキスト・ラベル・非活性 */
  GRAY: "#888888",
  /** 薄いボーダー・非選択マーカー枠 */
  MUTED: "#a1a1a1",
  /** 標準ボーダー・区切り線 */
  BORDER: "#ebebeb",
  /** 面の背景(薄グレー) */
  BG: "#f5f5f5",
  /** さらに薄い背景 */
  FAINT: "#fafafa",
  /** 白背景・反転前景("#fff" / "#ffffff" を統合) */
  WHITE: "#ffffff",
  /** アクセント(リンク・アクティブ・集合ピン) */
  BLUE: "#0070f3",
  /** エラー・危険・退出 */
  ERR: "#ee0000",
  /** 注意テキスト(公開バッジ等) */
  AMBER: "#ab570a",
  /** 注意背景(公開バッジの地色) */
  AMBER_BG: "#ffefcf",
} as const;

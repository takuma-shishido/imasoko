# サーバー設定の上書き(任意)。実 .env は .gitignore(docs/02 §3)。
# 有効期限オフセット(秒)。既定 10800 = 3時間(集合時間 + 3時間で自動終了。issue #4)
IMASOKO_END_OFFSET_SECONDS=10800
# 静的配信ディレクトリ(本番は apps/web の dist をここへコピー)
IMASOKO_STATIC_DIR=static
